[package]
name = "system-stats"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
ratatui = "0.30.2"
sysinfo = { version = "0.39.5", default-features = false, features = ["system", "disk"] }
